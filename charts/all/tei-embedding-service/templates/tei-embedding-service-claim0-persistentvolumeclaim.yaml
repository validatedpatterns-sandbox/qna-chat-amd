apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: tei-embedding-service-claim0
  name: tei-embedding-service-claim0
  namespace: {{ .Values.global.amdllm.namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.global.amdllm.tei_embedding.pvc.size }}
